---
alwaysApply: true
---

## PROJECT RULES - REWRITTEN FOR CLARITY AND COMPLIANCE

### **SYSTEM ENVIRONMENT**
- **ROS2 Version**: This is a ROS2 Humble workspace
- **Languages**: Python or C++ only
- **ROS2 Setup**: Always ensure ROS2 packages are sourced before operations
- **Hardware**: Jetson Orin Nano CPU
- **Robot Model**: Yahboomcar X3 with S2 lidar and Intel Realsense D435i camera
- **Test Files**: All test files should start with `test_`

### **CRITICAL SAFETY AND OPERATIONAL RULES**
- **ERROR MONITORING**: ALWAYS check terminal output for errors when running any programs
- **GRACEFUL SHUTDOWN**: Always use Ctrl+C to stop ROS2 nodes, never use `kill -9`
- **SYSTEM RESTART PROTOCOL**: Before rebooting or restarting the system, ALWAYS ask the user to stop ROS2 nodes first (this is a user preference)
- **BUILD METHOD**: Build ROS2 packages WITHOUT `--symlink-install` flag

### **WORKSPACE CONTEXT**
- **Current Workspace**: `/home/mw/yahboomcar_ros2/yahboomcar_ws`
- **Related Workspaces**:
  - `~/yahboomcar_ros2/yahboom_X3_lib` - Contains rosmaster library
  - `~/yahboomcar_ros2/library_ws` - Contains lidar packages
- **Client Architecture**: Client runs on separate AMD64 Ubuntu 22.04 machine connected via SSH
- **Communication Model**: All communications with client are REQUESTED BY THE CLIENT (pull system only)

### **OPERATIONAL CHECKLIST**
**BEFORE any robot operations:**
- ✅ ROS2 environment sourced?
- ✅ Robot hardware ready?
- ✅ No conflicting processes running?

**AFTER any operations:**
- ✅ Terminal output checked for errors?
- ✅ Resources properly cleaned up?
- ✅ No orphaned processes?

### **COMMAND EXECUTION GUIDELINES**
- **Path References**: Prefer absolute paths over relative paths
- **Background Processes**: Use background execution for long-running ROS2 nodes
- **Error Handling**: Always verify command success and check for error messages
- **Resource Management**: Clean up after operations to prevent conflicts

### **MEMORY-BASED PREFERENCES**
- **System Restart Protocol**: User prefers to be asked before system reboots/restarts to allow manual node shutdown

---

## **MY COMMITMENT TO THESE RULES**

I will:
1. **Always verify ROS2 environment** before running commands
2. **Check terminal output for errors** after every command execution
3. **Use graceful shutdown methods** (Ctrl+C) for ROS2 nodes
4. **Ask permission before system restarts** to allow manual node shutdown
5. **Build packages correctly** without --symlink-install
6. **Monitor for errors** and report them immediately
7. **Use absolute paths** when possible for clarity
8. **Follow the pull communication model** - only respond to user requests

These rules ensure safe, predictable, and reliable robot operations while respecting user preferences and system constraints.